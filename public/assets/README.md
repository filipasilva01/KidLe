# Local visual assets

Export the original illustrations from Figma into this folder before publishing the application. Replace the temporary Figma MCP URLs in `src/App.tsx` and `src/styles.css` with paths such as `/assets/cover.png`.
